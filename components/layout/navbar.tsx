"use client";

const MainNavBar = () => {
    return (
        <div>
            Enter
        </div>
    );
}

export default MainNavBar;

// This component seems unused or minimal. 
// If it's intended to be the Store Header, use <StoreHeader /> instead.